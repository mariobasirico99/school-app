/**
 * It contains path literals. It's used for
 * route management in {@link AppRoutingModule}
 *
 * @enum
 * @category Enum
 */
export enum Path {
  Login = 'login',
  Settings = 'settings',
  Home = 'home',
  Add_Card = 'add-card',
  Edit = 'edit'
}
