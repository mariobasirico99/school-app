/**
 * This json constant contains every configuration parameter about the production environment
 * @param production Indicate if it is production (```true```) or environment (```false```) environment
 * @param apiUrl: Indicate the Url to reach the Back End Api Service
 */

export const environment = {
  production: true,
  apiUrl: 'http://localhost:4000',
};
